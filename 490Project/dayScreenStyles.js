import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  dateText: {
    marginTop: 15,
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
